#pragma once

#include <LSystem/LSystem.hpp>

/*
class LSystemWindow
{
public:

	LSystemWindow(LSystem::LSystem* lsystem);

	void Draw();

	LSystem::Rule* ActiveRule();

private:

	void DrawRule(LSystem::Rule*);

	LSystem::LSystem* m_lsystem;
	LSystem::Rule* m_activeRule;

};
*/

